/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GoodsSkuInfoService } from "../goodsSkuInfo.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GoodsSkuInfoCreateInput } from "./GoodsSkuInfoCreateInput";
import { GoodsSkuInfoWhereInput } from "./GoodsSkuInfoWhereInput";
import { GoodsSkuInfoWhereUniqueInput } from "./GoodsSkuInfoWhereUniqueInput";
import { GoodsSkuInfoFindManyArgs } from "./GoodsSkuInfoFindManyArgs";
import { GoodsSkuInfoUpdateInput } from "./GoodsSkuInfoUpdateInput";
import { GoodsSkuInfo } from "./GoodsSkuInfo";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class GoodsSkuInfoControllerBase {
  constructor(
    protected readonly service: GoodsSkuInfoService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "GoodsSkuInfo",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: GoodsSkuInfo })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: GoodsSkuInfoCreateInput
  ): Promise<GoodsSkuInfo> {
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        goodsCode: true,
        goodsName: true,
        id: true,
        productCode: true,
        productId: true,
        productName: true,
        qrCode: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "GoodsSkuInfo",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [GoodsSkuInfo] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(GoodsSkuInfoFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<GoodsSkuInfo[]> {
    const args = plainToClass(GoodsSkuInfoFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        goodsCode: true,
        goodsName: true,
        id: true,
        productCode: true,
        productId: true,
        productName: true,
        qrCode: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "GoodsSkuInfo",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: GoodsSkuInfo })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: GoodsSkuInfoWhereUniqueInput
  ): Promise<GoodsSkuInfo | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        goodsCode: true,
        goodsName: true,
        id: true,
        productCode: true,
        productId: true,
        productName: true,
        qrCode: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "GoodsSkuInfo",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: GoodsSkuInfo })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: GoodsSkuInfoWhereUniqueInput,
    @common.Body() data: GoodsSkuInfoUpdateInput
  ): Promise<GoodsSkuInfo | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          goodsCode: true,
          goodsName: true,
          id: true,
          productCode: true,
          productId: true,
          productName: true,
          qrCode: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "GoodsSkuInfo",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: GoodsSkuInfo })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: GoodsSkuInfoWhereUniqueInput
  ): Promise<GoodsSkuInfo | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          goodsCode: true,
          goodsName: true,
          id: true,
          productCode: true,
          productId: true,
          productName: true,
          qrCode: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
